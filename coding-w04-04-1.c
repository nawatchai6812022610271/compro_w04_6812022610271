#include <stdio.h>

int main() {
    int score = 90.5;  
    printf("Score: = %d\n", score);
    return 0;
}
//code error เพราะ 90.5เป็นทศนิยม แต่intไม่สามาราถเก็บทศนิยมได้
// ทำไมต้องเลือก Data Type
// จะว่าจะทำให้โปรแกรไม่ผิดพลาดหรือ error
// อาจหาให้เสียข้อมูลได้ เช่น การเก็บทศนิยมในตัวแปร Int ไม่รองกับทศนิยม
// อภิปาย และสรุปผลทีได้จากการทดลอง
// Int ในมรองรับพนนียมได้ เพราะทาให้เก็ต error
// float รองวันทดนียมได้เพราะ Float เป็นตัวแปรที่ใช้เก็บทสนิยม     