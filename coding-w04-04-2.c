#include <stdio.h>

int main() {
    float score = 90.5;
    printf("Score: = %.1f\n", score);
    return 0;
}
//code ทำงานได้ปกติเพราะ 90.5เป็นdouble และใส่ F ด้านหลัง 90.5 เพื่อระบุว่าเป็น Float